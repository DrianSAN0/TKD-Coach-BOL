# Copiar este archivo a ".env" en la carpeta backend/ y completar con tus datos.
# Debe coincidir con las credenciales definidas en docker-compose.yml (raíz del proyecto).

DATABASE_URL=postgresql://tkduser:tkd1234@localhost:5432/tkd_coach_bol
